{
  "name": "propertysauce",
  "version": "1.0.0",
  "private": true,
  "description": "Property Sauce website: static site, brand package and AI tenant assistant.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "audit": "node audit.mjs",
    "dev": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0"
  },
  "engines": { "node": ">=20" }
}
